// nextflow.config

process {
    executor = 'local'
}

process {
    withName: makePredictions {
        executor = 'slurm'
        queue = 'gpu'
        clusterOptions = '--account=bgmp --gpus=1 --constraint=gpu-10gb'
        cpus = 32
        time = '3h'
    }
}